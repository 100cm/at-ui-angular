import { HollowDirective } from './hollow.directive';

describe('HollowDirective', () => {
  it('should create an instance', () => {
    const directive = new HollowDirective();
    expect(directive).toBeTruthy();
  });
});
