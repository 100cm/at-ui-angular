import { AtFormErrorDirective } from './at-form-error.directive';

describe('AtFormErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new AtFormErrorDirective();
    expect(directive).toBeTruthy();
  });
});
