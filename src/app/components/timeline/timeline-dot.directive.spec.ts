import { TimelineDotDirective } from './timeline-dot.directive';

describe('TimelineDotDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineDotDirective();
    expect(directive).toBeTruthy();
  });
});
