import { TimelineSelectorDirective } from './timeline-selector.directive';

describe('TimelineSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
