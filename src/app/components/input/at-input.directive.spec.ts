import { AtInputDirective } from './at-input.directive';

describe('AtInputDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new AtInputDirective();
    expect(directive).toBeTruthy();
  });
});
