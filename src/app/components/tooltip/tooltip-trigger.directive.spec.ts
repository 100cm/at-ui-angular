import { TooltipTriggerDirective } from './tooltip-trigger.directive';

describe('TooltipTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
