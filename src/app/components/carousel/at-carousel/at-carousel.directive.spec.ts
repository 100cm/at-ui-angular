import { AtCarouselDirective } from './at-carousel.directive';

describe('AtCarouselDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new AtCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
