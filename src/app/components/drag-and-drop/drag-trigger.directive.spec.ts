import { DragTriggerDirective } from './drag-trigger.directive';

describe('DragTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new DragTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
