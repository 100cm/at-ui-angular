import { AtI18nPipe } from './at-i18n.pipe';

describe('AtI18nPipe', () => {
  it('create an instance', () => {
    // @ts-ignore
    const pipe = new AtI18nPipe();
    expect(pipe).toBeTruthy();
  });
});
