import { AtTabInkDirective } from './at-tab-ink.directive';

describe('AtTabInkDirective', () => {
  it('should create an instance', () => {
    const directive = new AtTabInkDirective();
    expect(directive).toBeTruthy();
  });
});
