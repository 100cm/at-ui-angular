import { AtBreadItemDirective } from './at-bread-item.directive';

describe('AtBreadItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AtBreadItemDirective();
    expect(directive).toBeTruthy();
  });
});
