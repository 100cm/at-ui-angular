{
  "$schema": "http://json-schema.org/schema",
  "id": "ngAdd",
  "title": "add at-ng",
  "type": "object",
  "properties": {
    "skipPackageJson": {
      "type": "boolean",
      "default": false,
      "description": "Do not add at-ng dependencies to package.json (e.g., --skipPackageJson)"
    },
    "project": {
      "type": "string",
      "description": "Name of the project to target."
    }
  },
  "required": []
}
